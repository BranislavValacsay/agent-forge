import { Braces, CalendarClock, MousePointer2, Webhook } from 'lucide-react'
import { useI18n } from '../i18n'

export function TriggersPage(){const {t}=useI18n();return <div className="page"><header className="page-header"><div><p className="eyebrow">{t('triggers.eyebrow')}</p><h1>{t('triggers.title')}</h1><p>{t('triggers.description')}</p></div></header><div className="trigger-grid"><Trigger icon={<MousePointer2/>} type={t('trigger.manual')} title={t('triggers.manualTitle')} text={t('triggers.manualText')}/><Trigger icon={<CalendarClock/>} type={t('trigger.cron')} title={t('triggers.cronTitle')} text={t('triggers.cronText')}/><Trigger icon={<Webhook/>} type={t('trigger.api')} title={t('triggers.apiTitle')} text={t('triggers.apiText')}/></div><section className="panel trigger-note"><Braces size={18}/><div><h3>{t('triggers.noteTitle')}</h3><p>{t('triggers.noteText')}</p></div></section></div>}
function Trigger({icon,type,title,text}:{icon:React.ReactNode;type:string;title:string;text:string}){return <article className="trigger-card"><div className="trigger-icon">{icon}</div><p className="eyebrow">{type}</p><h2>{title}</h2><p>{text}</p></article>}
