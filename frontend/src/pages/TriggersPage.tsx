import { Braces, CalendarClock, MousePointer2, Webhook } from 'lucide-react'

export function TriggersPage(){return <div className="page"><header className="page-header"><div><p className="eyebrow">AUTOMATION</p><h1>Triggery</h1><p>Trigger pridávaš ako prvý node priamo v pipeline builderi.</p></div></header><div className="trigger-grid"><Trigger icon={<MousePointer2/>} type="MANUAL" title="Ručné spustenie" text="Používateľ spustí pipeline a podľa input schémy zadá text, JSON, obrázok alebo súbor."/><Trigger icon={<CalendarClock/>} type="CRON" title="Plánované spustenie" text="Cron výraz, timezone a overlap policy sú súčasťou konfigurácie trigger node."/><Trigger icon={<Webhook/>} type="CALLED / API" title="Externé zavolanie" text="Pipeline sa spustí cez autentifikovaný API endpoint s validovaným request payloadom."/></div><section className="panel trigger-note"><Braces size={18}/><div><h3>Žiadne globálne falošné triggery</h3><p>Táto stránka vysvetľuje podporované mechanizmy. Reálne triggery vzniknú až uložením konkrétnej pipeline.</p></div></section></div>}
function Trigger({icon,type,title,text}:{icon:React.ReactNode;type:string;title:string;text:string}){return <article className="trigger-card"><div className="trigger-icon">{icon}</div><p className="eyebrow">{type}</p><h2>{title}</h2><p>{text}</p></article>}
